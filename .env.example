# docker-compose 会自动读取本文件（与 Docker-compose.yml 同目录的 .env）。
# 复制为 .env 并填写，避免在 compose 文件里硬编码密码。

# MySQL root 密码（后端同样以 root 连接，故共用此密码）
MYSQL_ROOT_PASSWORD=

# JWT 密钥（至少 32 字节，可用 `openssl rand -hex 32` 生成）
JWT_SECRET=
