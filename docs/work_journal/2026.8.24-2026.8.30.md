# 2026.8.24
## 知识点
- md:学习md格式的文件的撰写。

- flyway:数据库迁移工具，在项目中配置依赖并在src/resource/db/migration下
写对应的文件如V1.1__描述.sql，配置数据源的链接，启动时springboot会自动执行
迁移工作，过程大致是扫描到在约定目录下存在sql语句，按照版本号依次执行，并在
对应数据库下建立flyway_schema_history表，记录版本历史号。

- redis:学习如何在javaspringboot项目里面使用redis，通过cache系列的注解在
service层的实现加上注解，可以字段级更新，但是项目未使用，采用的方案是一旦涉
及到缓存数据被更新则evict。

- swagger:配置springdoc的依赖，在application中添加环境变量，在security当中
放行ui页面的路径。

## 完成
- redis缓存配置，为项目添加redis缓存。
- 在config里给swagger界面添加简要的说明，目前暂未在controller中添加tag，
opration等注解。

# 2026.8.24

## 知识点
- swagger:详细看了swagger的annotation，tag(name,desription)，operation
(summary,description)，parameter(hidden,required,example,desription)，
感觉可以作为接口层的注释.
- junit:了解什么是单元测试，集成测试，端到端测试，主要了解了单元测试，test，
mock，injectmocks，displayname注解等，when，assert方法等。

## 完成
- 在接口层添加详细的swagger的annotation。
- 修复立即播放时当前播放歌曲被排序在后一首的逻辑，改为立即播放则当前歌曲位置
为上一首。
- 同意前后端删除添加歌曲到队列时对pos的处理，修复了前后端pos不一致的问题。
- 练习unit test，同时增加几个unit test。