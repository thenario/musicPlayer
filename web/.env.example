# ===== 前端环境变量 =====
# 后端访问根地址。
# 留空（推荐）= 相对路径：接口走同源 /api，静态资源走同源 /static。
#   - 开发：Vite 把 /api、/static 代理到后端（见 vite.config.ts 的 server.proxy）
#   - 生产：Nginx 处理 /api 转发与 /static 静态文件
# 填绝对地址 = 绕过代理直接访问（仅当不使用代理时），如 http://localhost:8080
VITE_API_URL=
